services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: sentryguard:latest
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./artifacts:/app/artifacts:ro

